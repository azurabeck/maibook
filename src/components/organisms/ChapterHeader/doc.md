# ChapterHeader

Importa um modelo salvo em `Estruturas → Cabeçalho` para o capítulo ativo. O modelo visual é copiado para o capítulo e somente os textos ficam editáveis.

```tsx
<ChapterHeader projectId={projectId} value={chapter.header ?? null} onChange={updateHeader} />
```
