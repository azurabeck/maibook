// Esta aba ainda é um placeholder — vamos evoluir ela juntos depois,
// seguindo o mesmo padrão da WriteTab: buscar dados do Firestore,
// guardar no zustand (ou num state local, se for só desta aba),
// e renderizar uma lista + formulário de criação/edição.

export function CharactersTab() {
  return (
    <div>
      <h2>🧑 Personagens</h2>
      <p>Em breve: lista de personagens, ficha de cada um e ajuda da IA para criar perfis.</p>
    </div>
  )
}
